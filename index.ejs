<!DOCTYPE html>
<html lang="en">
<body class="container">
  <table>
    <% waves.forEach( function(wave) { %>
       <tr>
         <td><%= wave.date %></td>
         <td><%= wave.name %></td>
         <td><%= wave.temps %></td>
       </tr>
    <% }) %>
</table>
</body>
</html>
